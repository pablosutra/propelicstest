//Currently usless, but if the app growths this controller will be helpful
"use strict";
var app = angular.module('PurchasedCtrl', []);


//Star of wishList
app.controller('PurchasedCtrl', ['$scope', '$rootScope',
    function($scope, $rootScope) {
        
    }
]);